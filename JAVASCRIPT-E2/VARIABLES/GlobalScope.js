{
    var a =20;
    
    let b=10;
    const c=50;
}
//! var is having global scope so that we can use it inside the block and  outside the block also
console.log(a);

//! let and const can not be access outside the block
console.log(b);
